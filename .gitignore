# Android build outputs
build/
app/build/
*.apk
*.aab

# Gradle
.gradle/
!gradle/wrapper/gradle-wrapper.jar

# Local machine config (SDK path etc.)
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Model file (too large for git — deploy via adb push)
*.litertlm
